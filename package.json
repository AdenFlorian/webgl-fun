{
  "name": "webgl-fun",
  "version": "0.0.1",
  "main": "index.js",
  "author": "David Valachovic \"AdenFlorian\"",
  "license": "MIT",
  "scripts": {
    "start": "yarn clean && yarn build:local --watch",
    "build:local": "tsc --build --verbose ./tsconfig.json",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "gl-matrix": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^3.7.2"
  }
}
